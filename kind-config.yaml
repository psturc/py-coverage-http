---
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30080  # NodePort from the service
    hostPort: 8080        # Host port to access from localhost
    protocol: TCP